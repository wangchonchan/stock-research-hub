version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./research_data_*.json:/app/research_data_*.json
    restart: unless-stopped
